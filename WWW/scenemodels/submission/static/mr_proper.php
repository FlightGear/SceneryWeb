<?php

$target_path = 'submission_3963abb99cbb640b';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_839640d03713ebcb';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_a25bd123f19fd8ac';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_55a6b793b965a48c';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_9399b31b2a67bfd4';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_bd57ff56ca27e062';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_20925b0167b88a55';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_768ffb28983fddc7';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_9f32955c9681d95f';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

$target_path = 'submission_ea7ab4d688ebe0f2';
unlink($target_path.'/submitted_files.tar.gz');  // Deletes compressed file
clearDir($target_path);

?>
